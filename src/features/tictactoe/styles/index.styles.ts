import { createStyles } from '@mui/styles';

export const styles = () =>
  createStyles({
    container: {
      flexGrow: 1,
      paddingTop: '5%',
      paddingLeft: '15%',
      paddingRight: '15%',
    },
  });
